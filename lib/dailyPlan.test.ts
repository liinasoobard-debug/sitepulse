import assert from "node:assert/strict"; import test from "node:test";
import { achievementRag, allocationActual, expectedGangOutput, nextWorkingDay, targetMessage, targetProductivityFactor, targetRag, type DailyPlanAllocation } from "./dailyPlan.ts";
const base={id:"a",project_id:"p",plan_date:"2026-08-13",gang_id:"g",gang_name:"Gang A",programme_activity_external_id:"A",building:"B",elevation:"S",level:"3",planned_operatives:6,target_quantity:48,unit:"m2",planned_man_day_productivity:7,expected_gang_output:42,target_productivity_factor:.875,target_rag:"GREEN",readiness_status:"READY",readiness_rag:"GREEN",readiness_snapshot:{},plan_status:"COMMITTED",revision_number:1,created_at:"now"} satisfies DailyPlanAllocation;
test("daily target formulas preserve lower-is-better PF",()=>{assert.equal(expectedGangOutput(7,6),42);assert.equal(targetProductivityFactor(42,42),1);assert.equal(targetProductivityFactor(42,50),.84);assert.equal(targetProductivityFactor(42,30),1.4);assert.equal(targetRag(1),"GREEN");assert.equal(targetRag(1.05),"AMBER");assert.equal(targetRag(1.4),"RED");assert.match(targetMessage(42,50,.84),/Stretch/)});
test("daily achievement and actual PF remain independent",()=>{const actual=allocationActual(base,[{id:"e",crewId:"g",programmeActivityId:"A",time:"08:00",duration:480,title:"Work",type:"work",status:"completed",quantity:44,affectedOperativeIds:["1","2","3","4","5","6"]}]);assert.equal(actual.achievementRag,"AMBER");assert.equal(actual.productivityRag,"green");assert.equal(actual.productivityFactor,42/44)});
test("carry forward selects next working day",()=>{assert.equal(nextWorkingDay("2026-08-14"),"2026-08-17");assert.equal(achievementRag(48,48),"GREEN")});
